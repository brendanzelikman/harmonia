import { Pose } from "types/Pose";
import { WholeNoteTicks } from "utils/durations";

export const FinitelyUpOneInversion: Pose = {
  id: "preset-finitely-up-one-inversion",
  name: "Up One Inversion",
  stream: [{ vector: { chordal: 1 }, duration: WholeNoteTicks }],
};

export const FinitelyUpTwoInversions: Pose = {
  id: "preset-finitely-up-two-inversions",
  name: "Up Two Inversions",
  stream: [{ vector: { chordal: 2 }, duration: WholeNoteTicks }],
};

export const FinitelyUpThreeInversions: Pose = {
  id: "preset-finitely-up-three-inversions",
  name: "Up Three Inversions",
  stream: [{ vector: { chordal: 3 }, duration: WholeNoteTicks }],
};

export const FinitelyUpFourInversions: Pose = {
  id: "preset-finitely-up-four-inversions",
  name: "Up Four Inversions",
  stream: [{ vector: { chordal: 4 }, duration: WholeNoteTicks }],
};

export const FinitelyUpFiveInversions: Pose = {
  id: "preset-finitely-up-five-inversions",
  name: "Up Five Inversions",
  stream: [{ vector: { chordal: 5 }, duration: WholeNoteTicks }],
};

export const FinitelyUpSixInversions: Pose = {
  id: "preset-finitely-up-six-inversions",
  name: "Up Six Inversions",
  stream: [{ vector: { chordal: 6 }, duration: WholeNoteTicks }],
};

export const FinitelyUpSevenInversions: Pose = {
  id: "preset-finitely-up-seven-inversions",
  name: "Up Seven Inversions",
  stream: [{ vector: { chordal: 7 }, duration: WholeNoteTicks }],
};

export const FinitelyUpEightInversions: Pose = {
  id: "preset-finitely-up-eight-inversions",
  name: "Up Eight Inversions",
  stream: [{ vector: { chordal: 8 }, duration: WholeNoteTicks }],
};

export const FinitelyUpNineInversions: Pose = {
  id: "preset-finitely-up-nine-inversions",
  name: "Up Nine Inversions",
  stream: [{ vector: { chordal: 9 }, duration: WholeNoteTicks }],
};

export const FinitelyUpTenInversions: Pose = {
  id: "preset-finitely-up-ten-inversions",
  name: "Up Ten Inversions",
  stream: [{ vector: { chordal: 10 }, duration: WholeNoteTicks }],
};

export const FinitelyUpElevenInversions: Pose = {
  id: "preset-finitely-up-eleven-inversions",
  name: "Up Eleven Inversions",
  stream: [{ vector: { chordal: 11 }, duration: WholeNoteTicks }],
};

export const FinitelyUpTwelveInversions: Pose = {
  id: "preset-finitely-up-twelve-inversions",
  name: "Up Twelve Inversions",
  stream: [{ vector: { chordal: 12 }, duration: WholeNoteTicks }],
};

export default {
  FinitelyUpOneInversion,
  FinitelyUpTwoInversions,
  FinitelyUpThreeInversions,
  FinitelyUpFourInversions,
  FinitelyUpFiveInversions,
  FinitelyUpSixInversions,
  FinitelyUpSevenInversions,
  FinitelyUpEightInversions,
  FinitelyUpNineInversions,
  FinitelyUpTenInversions,
  FinitelyUpElevenInversions,
  FinitelyUpTwelveInversions,
};
