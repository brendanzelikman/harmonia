import { Pose } from "types/Pose";
import { WholeNoteTicks } from "utils/durations";

export const FinitelyDownOneInversion: Pose = {
  id: "finitely-down-one-inversion",
  name: "Down One Inversion",
  stream: [{ vector: { chordal: -1 }, duration: WholeNoteTicks }],
};

export const FinitelyDownTwoInversions: Pose = {
  id: "finitely-down-two-inversions",
  name: "Down Two Inversions",
  stream: [{ vector: { chordal: -2 }, duration: WholeNoteTicks }],
};

export const FinitelyDownThreeInversions: Pose = {
  id: "finitely-down-three-inversions",
  name: "Down Three Inversions",
  stream: [{ vector: { chordal: -3 }, duration: WholeNoteTicks }],
};

export const FinitelyDownFourInversions: Pose = {
  id: "finitely-down-four-inversions",
  name: "Down Four Inversions",
  stream: [{ vector: { chordal: -4 }, duration: WholeNoteTicks }],
};

export const FinitelyDownFiveInversions: Pose = {
  id: "finitely-down-five-inversions",
  name: "Down Five Inversions",
  stream: [{ vector: { chordal: -5 }, duration: WholeNoteTicks }],
};

export const FinitelyDownSixInversions: Pose = {
  id: "finitely-down-six-inversions",
  name: "Down Six Inversions",
  stream: [{ vector: { chordal: -6 }, duration: WholeNoteTicks }],
};

export const FinitelyDownSevenInversions: Pose = {
  id: "finitely-down-seven-inversions",
  name: "Down Seven Inversions",
  stream: [{ vector: { chordal: -7 }, duration: WholeNoteTicks }],
};

export const FinitelyDownEightInversions: Pose = {
  id: "finitely-down-eight-inversions",
  name: "Down Eight Inversions",
  stream: [{ vector: { chordal: -8 }, duration: WholeNoteTicks }],
};

export const FinitelyDownNineInversions: Pose = {
  id: "finitely-down-nine-inversions",
  name: "Down Nine Inversions",
  stream: [{ vector: { chordal: -9 }, duration: WholeNoteTicks }],
};

export const FinitelyDownTenInversions: Pose = {
  id: "finitely-down-ten-inversions",
  name: "Down Ten Inversions",
  stream: [{ vector: { chordal: -10 }, duration: WholeNoteTicks }],
};

export const FinitelyDownElevenInversions: Pose = {
  id: "finitely-down-eleven-inversions",
  name: "Down Eleven Inversions",
  stream: [{ vector: { chordal: -11 }, duration: WholeNoteTicks }],
};

export const FinitelyDownTwelveInversions: Pose = {
  id: "finitely-down-twelve-inversions",
  name: "Down Twelve Inversions",
  stream: [{ vector: { chordal: -12 }, duration: WholeNoteTicks }],
};

export default {
  FinitelyDownOneInversion,
  FinitelyDownTwoInversions,
  FinitelyDownThreeInversions,
  FinitelyDownFourInversions,
  FinitelyDownFiveInversions,
  FinitelyDownSixInversions,
  FinitelyDownSevenInversions,
  FinitelyDownEightInversions,
  FinitelyDownNineInversions,
  FinitelyDownTenInversions,
  FinitelyDownElevenInversions,
  FinitelyDownTwelveInversions,
};
